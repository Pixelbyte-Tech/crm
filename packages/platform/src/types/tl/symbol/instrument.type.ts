export interface TlInstrument {
  name: string;
  id: string;
  groupName: string;
  groupId: string;
  description: string;
  parentGroupName: string;
  base: string;
  quote: string;
  lotSize: string;
  minLotSize: string;
  maxLotSize: string;
  pointValue: string;
  precision: string;
}
